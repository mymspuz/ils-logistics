import React from 'react'

const styleHeader: React.CSSProperties = {
    backgroundColor: '#eee',
    textAlign: 'center',
    color: '#000',
    fontSize: '24px',
    fontWeight: '600',
    textTransform: 'uppercase'
}

const styleContent: React.CSSProperties = {
    margin: '20px 50px'
}

const styleMap = {
    height: '350px'
}

const styleFooter: React.CSSProperties = {
    backgroundColor: '#eee',
    textAlign: 'center',
}

export { styleHeader, styleContent, styleMap, styleFooter }